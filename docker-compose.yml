services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - bims_data:/app/data
    env_file:
      - .env
    environment:
      - DATABASE_NAME=/app/data/db.sqlite3
    restart: always

volumes:
  bims_data:
